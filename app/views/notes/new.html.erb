<h1>Create New Note</h1>

<%= form_with(model: @note, local: true) do |form| %>
  <div>
    <%= form.label :content %>
    <%= form.text_area :content, rows: 10 %>
  </div>
  <div>
    <%= form.submit 'Submit' %>
  </div>
<% end %>
